# Checklist de cierre

- La estructura sigue `backend/` y `frontend/`.
- No se agrego NestJS.
- No se agrego pnpm.
- No se agrego Playwright/E2E.
- Las consultas backend nuevas filtran por `tenantId` cuando aplica.
- Los comandos ejecutados se reportan con resultado real.
- Si no se ejecuto Docker o base de datos, se informa claramente.
