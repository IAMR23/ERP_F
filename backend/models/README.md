# Modelos

El ERP usa Prisma en lugar de modelos Mongoose. Las entidades viven en:

```text
backend/prisma/schema.prisma
```

Esta carpeta se mantiene porque la arquitectura fisica sigue el estilo de `American`.
